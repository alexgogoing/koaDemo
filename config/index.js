const config = {
    // 启动端口
    port: 3010,
  
    // 数据库配置
    database: {
      DATABASE: 'nodesql',
      USERNAME: 'root',
      PASSWORD: 'jinxichen',
      PORT: '3306',
      HOST: 'localhost'
    }
  }
  
  module.exports = config;